# K ITIL deploiement

Procédure: docker compose up --build. Rollback: git checkout tag stable + docker compose down -v/up. Logs: docker compose logs backend/frontend/db.

Preuves associées: code source, captures d’écran, logs Docker, Swagger, tests, README et exports.
